#-*- coding: utf-8 -*-

# the minor version and change date gets automatically updated on a git commit
version=(0, 9, 'beta1')
last_changes="2013-09-13 18:00:00"

str_version=".".join(map(str, version))
